# ==============================================================================
# Copy to benchmark/benchmark.env and fill in external IPs, then run:
#   python3 benchmark/run_benchmark.py
# ==============================================================================

UNIFIED_RTX_ENDPOINT="http://<unified-rtx-node-external-ip>"
UNIFIED_L4_ENDPOINT="http://<unified-l4-node-external-ip>"

# Optional overrides
# REQUESTS_PER_WORKER=5
# INPUT_TOKENS=256
# OUTPUT_TOKENS=256
