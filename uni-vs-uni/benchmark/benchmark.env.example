# ==============================================================================
# Benchmark endpoints.
#
# RTX baseline (Vast.ai): keep this SSH tunnel open while benchmarking:
#   ssh -p 19270 root@99.148.65.9 -L 8080:localhost:8080
#
# L4 unified (GCP): unified-l4-node external IP (nginx :80).
# ==============================================================================

UNIFIED_RTX_ENDPOINT="http://127.0.0.1:8080"
UNIFIED_L4_ENDPOINT="http://<unified-l4-node-external-ip>"

# Optional overrides
# REQUESTS_PER_WORKER=5
# INPUT_TOKENS=256
# OUTPUT_TOKENS=256
